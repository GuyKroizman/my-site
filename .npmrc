registry=https://registry.npmjs.org/
# Security: Prevent dependency confusion attacks
# Only use official npm registry
# Lock down registry to prevent hijacking
save-exact=false
package-lock=true
# Disable scripts from untrusted sources (use with caution)
# ignore-scripts=false
